# yaml-language-server: $schema=https://www.schemastore.org/kustomization.json
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - knot-service.yaml
  - knot-stateful-set.yaml
  - knot-httproute.yaml

namespace: tangled-knot
