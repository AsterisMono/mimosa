# yaml-language-server: $schema=https://www.schemastore.org/kustomization.json
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - longhorn-local.yaml
  - longhorn-default-resource-cm.yaml
  - s3-backup-creds.yaml

namespace: longhorn-system
